buildscript {
    repositories {
        mavenCentral()
    }

    dependencies {
        classpath(libs.bundles.plugin.build.classpath)
    }
}

group = "co.kr.medipanda.portal"
version = "0.0.1-SNAPSHOT"